describe('TestProvider.estimateGas', () => {
  xit('can estimate a simple transfer')
  xit('can estimate a contract deploy')
  xit('can estimate a contract call')
  xit('can handle execution error')
})
